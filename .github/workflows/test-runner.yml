name: Test Self-Hosted Runner

on:
  workflow_dispatch:   # lets you trigger manually from GitHub

jobs:
  test:
    runs-on: [self-hosted]   # this will run on YOUR runner
    steps:
      - name: Say hello
        run: echo "Hello from my self-hosted runner on Windows!"
