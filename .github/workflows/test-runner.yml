name: Test Self-Hosted Runner

on:
  workflow_dispatch:   # allows you to trigger manually

jobs:
  test:
    runs-on: [self-hosted]   # this forces GitHub to use YOUR runner
    steps:
      - name: Say hello
        run: echo "Hello from my self-hosted runner on Windows!"
